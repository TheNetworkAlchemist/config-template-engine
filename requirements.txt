# Jinja2 Template Generator & Variable Builder — Python Dependencies
# Install with:  pip install -r requirements.txt
#
# Minimum Python version: 3.7
#
# variable_builder.py         — uses standard library only (no extra packages needed)
# jinja2_template_generator.py — requires the packages below
 
# Core templating engine
Jinja2>=3.1.0
 
# Data loading — CSV, XLS, XLSX
pandas>=2.0.0
 
# XLSX engine for pandas (Excel 2007+)
openpyxl>=3.1.0
 
# XLS engine for pandas (legacy Excel 97-2003)
xlrd>=2.0.1